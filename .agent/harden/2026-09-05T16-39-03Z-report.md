# Harden report — create/profile routing

## Proven invariants

| Invariant | Inputs tested | Result |
|---|---:|---|
| Keyed create rejects noncanonical material before callback | 1 native AbortSignal case | holds |
| Unkeyed create preserves opaque mapper values | 1 function plus 1 nested AbortSignal case | holds |
| Keyed create resists post-call mutation | 2 mutated fields | holds |
| Capability routing follows selected backend | 3 create routes and 3 recovery states | holds |

Evidence: agent-interface test 39 files and 515 tests passed.
Tangle test 16 files and 257 tests passed.
The four direct callback migrations passed 16 files and 221 tests.

## Remaining risk

| Surface | Risk | Status |
|---|---|---|
| Live Tangle runtime | This review did not call a live box.backend.status() | unverified deployment proof |
| Custom mapCreateInput | The mapper is trusted provider configuration, not untrusted request data | bounded by input validation and mapped-option checks |

No verified security exploit remains in this change set.

## Subsequent live failure: recovery is not qualified

The package tests above did not establish deployment compatibility.
[UP-09 failed on the published release](https://github.com/tangle-network/agent-sdk/actions/runs/33979254971/job/101341388401).
The recovered OpenCode session lacked `respondToInteraction`.
An owned staging sandbox reproduced HTTP 503 from `box.backend.status()` because OpenCode has no lifecycle service.
The SDK therefore lost the backend identity and removed both interaction and prompt capabilities.
The sandbox was deleted and its absence was confirmed.

The revised `scripts/fixtures/tangle-live-control.mjs` reconstructs OpenCode with a Codex provider default.
It checks recovered capabilities before inference, then checks a saved interaction response after reconstruction.
Running `TANGLE_SANDBOX_URL=https://staging-sandbox.tangle.tools node scripts/upstream-check.mjs UP-09` with authorized credentials failed before inference.
The first comparison observed `append: false` instead of `append: true`.
The fixture completed cleanup without a cleanup error.
Sandbox dollar cost was not captured.

Do not restore an assumed default, parse an error message for identity, or add a Lab identity cache.
The existing authenticated backend status operation must expose known identity without claiming unsupported lifecycle state.
Live qualification remains failed until this same check passes against the corrected deployment.
