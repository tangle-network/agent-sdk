---
"@tangle-network/agent-provider-cli-bridge": minor
---

Consume the CLI Bridge retained-session API with exact create and run digests, session-scoped wire run ids, canonical replay cursors, durable cancellation acknowledgements, owned-session cleanup, and one-shot fallback when retained creation cannot preserve caller input.
