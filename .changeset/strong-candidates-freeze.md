---
"@tangle-network/agent-interface": minor
---

Add the versioned `AgentCandidateBundle` wire contract for a strict frozen profile, content-addressed resources and code, typed secret references, shell-free execution, isolated memory, lineage and spend, RFC 8785 identity, plus materialization and run receipts.
