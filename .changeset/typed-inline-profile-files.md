---
"@tangle-network/agent-provider-tangle": patch
---

Allow inline profile file contents through Sandbox's existing deferred file transport.
Validate mounts before provisioning and preserve all other JSON and metadata limits.
