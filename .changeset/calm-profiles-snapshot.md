---
"@tangle-network/agent-interface": minor
---

Add the canonical `snapshotAgentProfile` intake function to detach, validate, and recursively freeze an exact `AgentProfile` without adding execution policy or defaults.
