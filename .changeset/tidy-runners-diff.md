---
"@tangle-network/agent-interface": patch
---

Add the profile harness preference to `AgentProfileDiff` set, removal, changed-axis, and pruning operations, while ensuring removals and pruned fields cannot affect unrelated profile axes.
