---
"@tangle-network/agent-interface": minor
---

Candidate tasks can now declare either a workspace change or a bounded media output.
Repository identity is tracked independently, and output receipts retain their media constraints.
The package now owns the v1 improvement proposal, review, measured comparison, and successful execution evidence schemas.
Those records bind the exact baseline and candidate bundle, preserve every held-out objective, carry full knowledge promotion identity, and include the materialization receipt plus native profile files used for execution.
