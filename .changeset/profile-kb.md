---
"@tangle-network/agent-interface": minor
---

Add the profile knowledge base at `@tangle-network/agent-interface/profile-kb`: sourced, dated guidance for the frontier harnesses and models the platform runs, `withProfileKb` to compose harness, model, and profile guidance into an `AgentProfile` prompt, and `composeAgentProfileGuidance` as the reusable composer.
