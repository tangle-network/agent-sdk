---
"@tangle-network/agent-provider-cli-bridge": patch
---

Disable response-header and body-idle timeouts by default, reuse connections across turns, and close provider resources with the environment.
