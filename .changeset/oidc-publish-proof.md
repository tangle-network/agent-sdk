---
"@tangle-network/agent-provider-testkit": patch
---

Publish through npm trusted publishing (OIDC): this release exists to prove the tokenless path end to end after every package was registered as a trusted publisher for `release.yml`. No code change.
